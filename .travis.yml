sudo: false

language: php

php:
  - hhvm
  - 7.1
  - 7.0
  - 5.6
  - 5.5

env:
  matrix:
    - PREFER_LOWEST="--prefer-lowest --prefer-stable"
    - PREFER_LOWEST=""

install:
  - composer install

before_script:
  - composer update $PREFER_LOWEST

script:
  - vendor/bin/phpunit
