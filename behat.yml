default:
    suites:
        process_runner:
            contexts:
                - FeatureContext:
                    args: ['--runner=process']
        eval_runner:
            contexts:
                - FeatureContext:
                    args: ['--runner=eval']
